# Project Memory — Index

> Repo-scoped memory the Fleet Overseer (and local agents) append to over time.
> Mirrors the `~/.claude` MEMORY.md pattern, but lives *in the project*.

## How to use

- Add durable, repo-specific learnings as dated entries in this directory
  (e.g. `2026-06-27-doppler-secrets.md`).
- Keep this index current: one line per entry, newest first.
- The 3–5 most critical facts also belong inline in `.project/manifest.yaml`
  under `pinned:` so the overseer always sees them without drilling in.

## Entries

_(none yet — this is a placeholder seed; append entries as you learn them.)_

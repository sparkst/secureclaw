# .project/manifest.yaml — the per-repo contract the Fleet Overseer reads.
#
# FACTS are deterministic and are rewritten by the freshness writer on every
# commit; never edit them by hand. SUMMARY and PINNED are the deliberate,
# human/LLM-authored layers — the placeholders below are meant to be replaced.

# ── FACTS (deterministic; rewritten by the freshness writer, never by hand) ──
schema_version: 1
name: secureclaw
repo: github.com/sparkst/secureclaw
last_commit: "2026-05-02T23:40Z  (fdd68a7)"
last_synced: "2026-05-02T23:40Z"
languages: [html, python, shell]
deps_changed: false
tests: {count: 0, last_run: '1970-01-01', status: unknown}
ci: {provider: unknown, status: unknown}
tree_digest: "ce49f7bf34e16333"
summary_stale: true

# ── SUMMARY (LLM-written; "deliberate" layer; refreshed on purpose) ──
summary: "PLACEHOLDER — run a deliberate SUMMARY refresh to replace this stub; summary_stale stays TRUE until a real summary is written."
architecture_doc: docs/architecture.md
status_doc: requirements/current.md

# ── PINNED MEMORY (3–5 things the overseer always needs) ──
pinned:
  active_goal: "PLACEHOLDER — set the active goal for this repo (TODO stub)."
  sharp_edges:
    - "PLACEHOLDER — list sharp edges / gotchas the overseer must respect (TODO stub)."
  do_not_touch:
    - "PLACEHOLDER — list paths the overseer must never modify, e.g. .env (TODO stub)."
memory_store: .project/memory/
